# Product Webhook Service

Node.js сервис для управления продуктами и обработки вебхуков. Приложение поддерживает создание и обновление продуктов с автоматической отправкой событий на зарегистрированные вебхуки.

## 📋 Требования

- **Node.js**: версия 14+
- **npm**: встроен с Node.js

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

Это установит необходимые пакеты:
- **express**: веб-фреймворк для создания API
- **axios**: HTTP-клиент для отправки запросов к вебхукам
- **nodemon**: автоматический перезапуск приложения при изменениях (для разработки)

### 2. Запуск проекта

**Для разработки (с автоперезагрузкой):**

```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

**Для продакшна:**

```bash
node app.js
```

### 3. Проверка работы

Откройте браузер и перейдите на `http://localhost:3000` — вы увидите веб-интерфейс.

## 📁 Структура проекта

```
laborator_nr5/
├── app.js                          # Главный файл приложения
├── db.js                           # In-memory база данных
├── package.json                    # Конфигурация проекта
├── public/                         # Статические файлы
│   ├── index.html                  # Веб-интерфейс
│   └── webhook-receiver.js         # Клиентский скрипт для демо
├── routes/                         # API маршруты
│   ├── product.routes.js           # Маршруты для продуктов
│   ├── webhook.routes.js           # Маршруты для регистрации вебхуков
│   └── webhook.receiver.routes.js  # Маршрут-приемник вебхуков
├── controllers/                    # Обработчики запросов
│   ├── product.controller.js       # Логика работы с продуктами
│   └── webhook.controller.js       # Логика регистрации вебхуков
├── models/                         # Модели данных
│   ├── product.model.js            # Модель продукта
│   └── webhook.model.js            # Модель вебхука
└── services/                       # Бизнес-логика
    └── webhook.service.js          # Сервис отправки вебхуков
```

## 🏗️ Архитектура приложения

### Слои приложения

1. **Routes** (`/routes`) - определяют API endpoints
2. **Controllers** (`/controllers`) - обрабатывают HTTP запросы и вызывают моделиServ
3. **Models** (`/models`) - работают с данными в БД
4. **Services** (`/services`) - реализуют бизнес-логику (например, отправка вебхуков)
5. **Database** (`db.js`) - хранит данные в памяти

### Поток данных

```
HTTP Request → Route → Controller → Model/Service → Database
                                    ↓
                            WebhookService → Axios → External Webhook URLs
```

## 🔌 API Endpoints

### Продукты

#### Создание продукта
```http
POST /products
Content-Type: application/json

{
  "name": "Ноутбук",
  "price": 1500
}
```

**Ответ:**
```json
{
  "message": "Product created",
  "product": {
    "id": "1701691234567",
    "name": "Ноутбук",
    "price": 1500
  }
}
```

При создании автоматически отправляется событие `product.created` всем зарегистрированным вебхукам.

#### Обновление продукта
```http
PUT /products/:id
Content-Type: application/json

{
  "name": "Новое название",
  "price": 2000
}
```

При обновлении отправляется событие `product.updated`.

### Вебхуки

#### Регистрация вебхука
```http
POST /webhooks/register
Content-Type: application/json

{
  "url": "https://example.com/webhook"
}
```

**Ответ:**
```json
{
  "message": "Webhook registered",
  "webhook": {
    "id": "1701691234567",
    "url": "https://example.com/webhook"
  }
}
```

#### Просмотр полученных событий
```http
GET /webhook-receiver
```

**Ответ:** Массив всех событий, полученных на endpoint `/webhook-receiver`

#### Получение события вебхука (демо приемник)
```http
POST /webhook-receiver
Content-Type: application/json

{
  "event": "product.created",
  "data": { "id": "123", "name": "Товар", "price": 100 }
}
```

## 🔄 Как работают вебхуки

1. **Регистрация**: клиент отправляет свой URL на `/webhooks/register`
   ```bash
   curl -X POST http://localhost:3000/webhooks/register \
     -H "Content-Type: application/json" \
     -d '{"url":"http://localhost:3001/my-webhook"}'
   ```

2. **Создание/обновление продукта**: когда происходит действие
   ```bash
   curl -X POST http://localhost:3000/products \
     -H "Content-Type: application/json" \
     -d '{"name":"Товар","price":100}'
   ```

3. **Триггер**: `WebhookService` автоматически отправляет POST запрос на все зарегистрированные URLs
   ```json
   {
     "event": "product.created",
     "data": { "id": "123", "name": "Товар", "price": 100 }
   }
   ```

4. **Обработка ошибок**: если вебхук недоступен, ошибка логируется, но приложение продолжает работу

## 📊 Пример использования

### Запуск локального приемника вебхуков

Создайте файл `webhook-client.js`:

```javascript
import express from 'express';
const app = express();
app.use(express.json());

app.post('/my-webhook', (req, res) => {
  console.log('📨 Webhook received:', req.body);
  res.json({ ok: true });
});

app.listen(3001, () => {
  console.log('Webhook receiver running on http://localhost:3001');
});
```

Запустите:
```bash
node webhook-client.js
```

Зарегистрируйте вебхук:
```bash
curl -X POST http://localhost:3000/webhooks/register \
  -H "Content-Type: application/json" \
  -d '{"url":"http://localhost:3001/my-webhook"}'
```

Создайте продукт и увидите событие в консоли приемника:
```bash
curl -X POST http://localhost:3000/products \
  -H "Content-Type: application/json" \
  -d '{"name":"Тестовый товар","price":50}'
```

## 🛠️ Основные компоненты

### `app.js`
- Инициализирует Express приложение
- Добавляет middleware для логирования запросов/ответов
- Подключает маршруты
- Запускает сервер на порту 3000

### `db.js`
- Простая in-memory база данных
- Хранит массивы продуктов и вебхуков

### `ProductController`
- `create()` - создает новый продукт и триггерит вебхук
- `update()` - обновляет продукт и триггерит вебхук

### `WebhookService`
- `trigger(event, payload)` - отправляет событие всем зарегистрированным вебхукам

### `ProductModel`
- `create()` - добавляет продукт в БД с уникальным ID
- `update()` - обновляет существующий продукт

### `WebhookModel`
- `register()` - регистрирует новый вебхук
- `getAll()` - возвращает все зарегистрированные вебхуки

## 📝 Особенности

- ✅ REST API для управления продуктами
- ✅ Система вебхуков для оповещений о событиях
- ✅ Логирование всех HTTP запросов/ответов
- ✅ Graceful обработка ошибок при отправке вебхуков
- ✅ In-memory база данных (данные теряются при перезагрузке)
- ✅ Автоматическая перезагрузка с nodemon во время разработки

## ⚠️ Ограничения

- Данные хранятся в памяти и теряются при перезагрузке сервера
- Нет аутентификации/авторизации
- Нет валидации Email для URL вебхуков
- Нет истории событий в основной БД

## 🔧 Дальнейшее развитие

- Добавить реальную БД (MongoDB, PostgreSQL)
- Реализовать очередь событий (Bull, RabbitMQ)
- Добавить retry логику для неудачных вебхуков
- Добавить аутентификацию
- Реализовать веб-интерфейс для управления

## 📞 Поддержка

При возникновении проблем проверьте:
1. Запущен ли сервер на порту 3000
2. Установлены ли все зависимости (`npm install`)
3. Правильные ли URL вебхуков (используйте публичные сервисы для тестирования, например webhook.site)


# Сравнение REST и WebHooks

## 1. Основные отличия REST от WebHooks

| Характеристика         | REST                                      | WebHooks                                     |
|------------------------|-------------------------------------------|---------------------------------------------|
| **Инициация запроса**   | Клиент инициирует запрос к серверу        | Сервер инициирует запрос к клиенту при событии |
| **Модель коммуникации** | Запрос–ответ                              | Push (сервер «толкает» данные клиенту)     |
| **Частота обновлений**  | Клиент периодически опрашивает сервер     | Обновления происходят мгновенно при событии |
| **Использование ресурсов** | Клиент постоянно делает запросы (polling) | Снижает нагрузку на сервер и сеть          |
| **Гибкость данных**      | Возвращает данные по фиксированным эндпоинтам | Клиент получает данные сразу, по событию   |

## 2. Преимущества WebHooks по сравнению с REST

- **Мгновенные уведомления:** сервер сразу отправляет данные клиенту, нет задержек опроса.  
- **Экономия ресурсов:** уменьшение числа ненужных запросов и нагрузки на сервер.  
- **Простота интеграции событий:** легко подключить сторонние сервисы (например, уведомления, логирование, обработка платежей).  
- **Гибкость:** клиент получает данные именно тогда, когда они становятся актуальными.  

## 3. Ограничения и недостатки WebHooks

- **Надёжность доставки:** возможны потери данных при проблемах сети; требуется повторная отправка или очередь сообщений.  
- **Безопасность:** необходимо проверять подписи или токены, чтобы убедиться, что запрос действительно от сервера.  
- **Отсутствие стандартного формата:** разные сервисы могут использовать разные структуры данных.  
- **Сложность дебага:** труднее тестировать и отслеживать события, особенно при большом объёме WebHooks.  

## 4. Интеграция WebHooks с существующей архитектурой

- **REST + WebHooks:** REST продолжает использоваться для основных CRUD операций, а WebHooks добавлены для уведомлений о событиях.  
- **База данных:** сервер отслеживает события в БД (например, создание заказа) и отправляет WebHook при изменении данных.  
- **Аутентификация:** клиент регистрирует URL WebHook, сервер проверяет токен или секрет при каждой отправке, чтобы гарантировать безопасность.  
- **Очередь сообщений:** для надёжности можно использовать очередь (например, Redis или RabbitMQ) для повторной отправки WebHooks при сбое.  
